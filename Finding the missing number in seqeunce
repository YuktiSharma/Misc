#include<stdio.h>
int main()
{
int a[10],i,n;
printf("enter the size of seq");
scanf("%d",&n);
printf("enter the sequence");
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
int d=2;
for(i=1;i<n;i++)
{
if(a[i]!=(a[i-1]+2))
printf("%d missing",a[i]-d);
}
return 0;
}

